# Neural Bridge API URL (Leave empty to use the built-in AI Studio backend)
VITE_API_URL=

# Firebase Admin SDK configuration (JSON string from Service Account)
FIREBASE_CONFIG=

# JWT Secret for signing local tokens (e.g. neuro-pass-xyz-123)
JWT_SECRET=
