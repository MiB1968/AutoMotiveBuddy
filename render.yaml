services:
  - type: web
    name: automotive-frontend
    env: static
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    branch: main
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
